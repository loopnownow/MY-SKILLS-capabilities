---
name: "design-protocol"
domain: "03_research"
trigger: ["写方案", "填方案", "protocol"]
outputs: ["protocol_draft"]
---

# Study protocol write / fill

Generic protocol text. Not experimental-design blueprints (`design-experiment/`) and not grant Voice A/B.

Backup: MedSci `write-protocol` / `fill-protocol`.
