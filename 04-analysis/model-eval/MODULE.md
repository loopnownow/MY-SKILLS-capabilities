---
name: "model-eval"
domain: "04_analysis"
trigger: ["校准", "DCA", "外验证", "模型评价"]
outputs: ["eval_report"]
---

# Model evaluation (calibration / DCA / external validation)

Not test selection (`stats-guide/`) and not fitting (`stats-models/`). Lab 0RAD / DeLong / Cox policy stays in A `04_analysis/personal/`.

Backup: MedSci `model-evaluation` / `model-validation`.
