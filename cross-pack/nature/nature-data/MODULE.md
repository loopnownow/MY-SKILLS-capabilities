<!--
POINTER ONLY — Architecture Batch 1 (de-entity).
Former MODULE body duplicated upstream skill text; removed.
Entity SSOT: MY-SKILLS `mounts-cap/nature/skills/nature-data/`
Do not re-vendor full skill trees into cross-pack/.
-->

# nature-data — pointer

This B `cross-pack` folder is a **stub**. The real skill entity lives in the A-side
cache at:

```text
mounts-cap/nature/skills/nature-data/
```

Load via A registry fine id whose `stub_in_b` points here. Do not copy scripts,
references, or full SKILL bodies into this tree.
