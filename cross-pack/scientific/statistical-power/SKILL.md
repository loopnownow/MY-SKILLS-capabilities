---
name: "statistical-power"
label_cn: "样本量-复杂设计模拟法(蒙特卡洛)"
mount_status: "cross_package_pointer"
source_package: "Scientific"
description: "POINTER only — entity lives in A mounts-cap cache (mounts-cap/scientific/skills/statistical-power/). Do not duplicate."
owner: "cross-pack/scientific/statistical-power/ — pointer stub"
---

# 样本量-复杂设计模拟法(蒙特卡洛)

**POINTER — do not duplicate the skill body here.**

| | |
|---|---|
| Entity (SSOT) | A's local cache: `mounts-cap/scientific/skills/statistical-power/` |
| Fetch | `python mounts-cap/fetch.py ensure --id <fine-id>` in MY-SKILLS |
| Registry | A `01_skill-discovery-integration/registry.yaml` (`stub_in_b: cross-pack/scientific/statistical-power/`) |
| This folder | Thin stub only (`SKILL.md` + `MODULE.md` note) |

Capability bytes belong in the external package cache, not in B `cross-pack/`.
