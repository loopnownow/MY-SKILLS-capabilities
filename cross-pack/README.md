# cross-pack/ — pointers only

Stubs for Scientific / Nature / AIPOCH hybrid mounts indexed by A `registry.yaml` (`stub_in_b`).

**Entity SSOT** is A's local cache: `mounts-cap/<scientific|nature|aipoch>/…`.
Do **not** duplicate full skill trees here. Each folder is a thin `SKILL.md` + `MODULE.md` pointer.

MedSci-native / B domain skills under `02-data-processing/`, `03-research/`, `04-analysis/`, `05-manuscript/`, `06-review/` are **not** pointers — leave those alone.
